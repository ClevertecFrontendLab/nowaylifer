import { defineStyleConfig } from '@chakra-ui/react';

export const Badge = defineStyleConfig({
    baseStyle: {
        color: 'black',
        textTransform: 'none',
        fontWeight: 'normal',
        fontSize: 'sm',
    },
});
